/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: 'oimkxnwjowrhbzkbcpbn.supabase.co',
      },
    ],
  },
};

export default nextConfig;
